# OS generated files
.DS_Store
Thumbs.db

# Node
node_modules/
npm-debug.log

# Build Assets
# We keep the compiled CSS for the user as per prompt "Bundled locally"
# But we might ignore cache
.sass-cache/

# IDEs
.vscode/
.idea/
*.sublime-workspace

# Local config
stubs.php
