{
  "name": "wp-performance-regression-detector",
  "version": "1.0.0",
  "description": "Build scripts for WPRD plugin",
  "scripts": {
    "build": "tailwindcss -i ./assets/tailwind.input.css -o ./assets/css/admin.css --minify",
    "watch": "tailwindcss -i ./assets/tailwind.input.css -o ./assets/css/admin.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
